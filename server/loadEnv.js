// import { DotenvConfigOptions } from "dotenv";
// load .env fi contents into process.env.
import dotenv from 'dotenv'

/*
dotenv.config({
	path: './custom/path',
	encoding: 'latin1',
	debug: true,
	override: false
})
*/
dotenv.config()
